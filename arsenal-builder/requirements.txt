# For scraping modern dynamic sites (Selenium)
selenium>=4.20.0

# For scraping legacy static HTML (BeautifulSoup)
beautifulsoup4>=4.12.3
requests>=2.31.0

# For loading API keys from .env (CategoryPredicter.py, add_github_urls.py)
python-dotenv>=1.0.1

# LLM category prediction (CategoryPredicter.py)
openai>=1.30.0
google-generativeai>=0.7.0

# Optional: JSON pretty print fallback
orjson; platform_system != "Windows"
